---
version: '2'
services:
  cache1:
    build:
      context: ./cache1
      args:
        DOCKER_HOST: $DOCKER_HOST
  cache2:
    build:
      context: ./cache2
      args:
        DOCKER_HOST: $DOCKER_HOST
    ports:
      - "80:8080"
  origin:
    build: ./origin
